<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>franka_streaming_impedance_controller</name>
  <version>0.1.0</version>
  <description>
    Streaming Cartesian impedance control for the Franka FR3: a ros2_control torque controller
    driven by absolutely-timed pose chunks, which it splices without stopping so a chunked
    reference (a policy's action chunks, a planner's path) survives to the joint level. Ships
    with a Franka Hand driver that talks to libfranka directly rather than through
    franka_gripper. Control law ported from serl_franka_controllers; reference generator ported
    from UMI's PoseTrajectoryInterpolator.
  </description>
  <maintainer email="anonymous@example.com">polyumi-authors</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>controller_interface</depend>
  <depend>eigen</depend>
  <depend>libfranka</depend>
  <depend>franka_msgs</depend>
  <depend>franka_semantic_components</depend>
  <depend>hardware_interface</depend>
  <depend>pluginlib</depend>
  <depend>rclcpp</depend>
  <depend>rclcpp_lifecycle</depend>
  <depend>realtime_tools</depend>
  <depend>sensor_msgs</depend>
  <depend>tf2_eigen</depend>
  <depend>tf2_ros</depend>
  <depend>trajectory_msgs</depend>

  <test_depend>ament_cmake_gtest</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
    <controller_interface plugin="${prefix}/franka_streaming_impedance_controller.xml"/>
  </export>
</package>
