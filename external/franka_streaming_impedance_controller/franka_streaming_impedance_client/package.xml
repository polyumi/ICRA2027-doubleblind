<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>franka_streaming_impedance_client</name>
  <version>0.1.0</version>
  <description>
    Producer-side helpers for franka_streaming_impedance_controller: builds the absolutely-timed
    MultiDOFJointTrajectory chunks the controller splices on, and wraps the publisher so a
    producer can tell whether the controller is actually subscribed.

    Pure Python and ROS-message-only, so it installs into any workspace on any distro the
    controller itself builds against.
  </description>
  <maintainer email="anonymous@example.com">polyumi-authors</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_python</buildtool_depend>

  <depend>geometry_msgs</depend>
  <depend>rclpy</depend>
  <depend>trajectory_msgs</depend>

  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
